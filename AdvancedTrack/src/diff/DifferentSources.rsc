@doc { Generate the diff between M3 models from different sources }

module diff::DifferentSources

// what do we need?
// - missing annotations
// - diff in annotations

public void run() {
	M3 model1 = "";
	M3 model2 = "";
}

public void CompareM3(model1, model2) {
	// annotations of m3 model
	
}